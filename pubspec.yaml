name: esc_pos_printer
description: The library allows to print receipts using an ESC/POS thermal WiFi printer.
version: 4.1.0
homepage: https://github.com/andrey-ushakov/esc_pos_printer
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  charset_converter: ^2.0.0
  esc_pos_utils:
    git:
      url: https://github.com/rithik20/esc_pos_utils_package.git
  flutter:
    sdk: flutter
  # esc_pos_utils:
  #   path: ../esc_pos_utils
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
